{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "test": { "dependsOn": ["^build"], "env": ["DATABASE_URL", "SUPABASE_JWKS_URL"] },
    "lint": {},
    "typecheck": { "dependsOn": ["^build"] }
  }
}